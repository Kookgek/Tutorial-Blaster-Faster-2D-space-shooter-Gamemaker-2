instance_destroy();
if(o_player_ship.laser_speed = 20){
other.armor -= 2;
audio_play_sound(a_hit, 2, false);
}

else if (o_player_ship.laser_speed = 10){
other.armor -= 1;
audio_play_sound(a_hit, 2, false);
}
